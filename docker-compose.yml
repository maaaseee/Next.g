services:
  db:
    image: postgres:16-alpine
    container_name: nextg-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
      POSTGRES_DB: nextg_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d nextg_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: nextg-backend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DATABASE_URL: ${DATABASE_URL:-postgres://postgres:postgrespassword@db:5432/nextg_db}
      RAWG_API_KEY: ${RAWG_API_KEY:-}
      CORS_ORIGIN: "http://localhost:5173"
      NODE_ENV: production
      AUTO_SEED: ${AUTO_SEED:-false}
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
